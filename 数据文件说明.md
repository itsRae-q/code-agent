# 数据文件说明文档

## 核心数据文件介绍

### 1. `fcf_index_results.csv` - 国证自由现金流指数筛选结果

这是本项目的核心输出文件，包含了经过完整筛选流程后的48只入选股票及其详细信息。

#### 文件结构
```csv
排名,股票代码,股票名称,所属行业,自由现金流率,权重,权重百分比,自由现金流(万元),经营现金流(万元),最新股价(元)
```

#### 字段说明

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| 排名 | 整数 | 按自由现金流率排序的名次 | 1, 2, 3... |
| 股票代码 | 字符串 | 6位股票代码 | 000015, 000168 |
| 股票名称 | 字符串 | 股票完整名称 | 测试股票015 |
| 所属行业 | 字符串 | 国证行业分类 | 电气设备, 制造业 |
| 自由现金流率 | 小数 | FCF/企业价值，排序依据 | 0.000055 |
| 权重 | 小数 | 在指数中的权重（0-1） | 0.0263 |
| 权重百分比 | 字符串 | 权重的百分比表示 | 2.63% |
| 自由现金流(万元) | 整数 | 最近一年自由现金流 | 28,502 |
| 经营现金流(万元) | 整数 | 最近一年经营现金流 | 33,994 |
| 最新股价(元) | 小数 | 最新收盘价 | 5.16 |

#### 数据特征分析

**权重分布**：
- 最大权重：3.88%（股票代码000200）
- 最小权重：0.52%（股票代码000182）
- 平均权重：2.08%
- 权重标准差：0.75%

**行业分布**：
- 医药生物行业占比最高：5只股票（10.4%）
- 煤炭、化工、机械设备：各4只股票（8.3%）
- 总计覆盖16个不同行业，分散度良好

**现金流质量**：
- 自由现金流总和：108.29亿元
- 经营现金流总和：166.08亿元
- 现金流转换率：65.2%（自由现金流/经营现金流）

### 2. `requirements.txt` - 项目依赖文件

包含运行本项目所需的Python包及版本要求。

```txt
pandas>=1.5.0      # 数据处理和分析
numpy>=1.21.0      # 数值计算
yfinance>=0.2.0    # 股票数据获取（可选）
akshare>=1.9.0     # 中国股市数据接口（可选）
tushare>=1.2.0     # 金融数据接口（可选）
requests>=2.28.0   # HTTP请求库
matplotlib>=3.6.0  # 数据可视化（可选）
seaborn>=0.12.0    # 统计数据可视化（可选）
openpyxl>=3.0.0    # Excel文件处理（可选）
```

### 3. 代码文件数据流说明

#### `simple_screener.py` - 主筛选程序
**输入数据**：
- 通过`data_generator.py`生成的模拟股票数据
- 包含200只股票的基础信息、财务数据、交易数据

**处理流程**：
1. 基础条件筛选（剔除ST股票）
2. 流动性筛选（成交金额排名）
3. 行业筛选（剔除金融房地产）
4. ROE稳定性筛选
5. 现金流条件筛选
6. 经营质量筛选
7. 自由现金流率计算和排序
8. 权重计算和标准化

**输出数据**：
- `fcf_index_results.csv`：最终筛选结果
- 控制台统计信息：详细的筛选过程数据

#### `data_generator.py` - 测试数据生成器
**生成数据类型**：

1. **基础信息数据**：
   ```python
   {
       'code': '000001',           # 股票代码
       'name': '测试股票001',       # 股票名称
       'industry': '制造业'         # 所属行业
   }
   ```

2. **交易数据**：
   ```python
   {
       'avg_trading_amount': 25000,  # 日均成交金额（万元）
       'latest_price': 15.68        # 最新股价（元）
   }
   ```

3. **财务数据**：
   ```python
   {
       'operating_cash_flow': 35000,  # 经营现金流（万元）
       'free_cash_flow': 28000,      # 自由现金流（万元）
       'roe_stability': 0.05,        # ROE稳定性（标准差）
       'net_profit': 20000          # 净利润（万元）
   }
   ```

### 4. 数据质量控制

#### 筛选条件验证
- **ST股票识别**：通过股票名称包含"ST"字符串判断
- **流动性要求**：日均成交金额排名前80%
- **行业限制**：排除银行、保险、证券、房地产行业
- **现金流质量**：要求自由现金流和经营现金流均为正值
- **ROE稳定性**：按12个季度ROE标准差排序，选取前90%

#### 权重计算规则
- **基础权重**：按自由现金流大小计算初始权重
- **权重上限**：单只股票权重不超过10%
- **标准化**：确保所有权重总和为100%
- **最小权重**：避免权重过小导致的交易成本问题

### 5. 使用建议

#### 数据更新频率
- **季度更新**：按照指数编制方案，建议每季度重新筛选
- **实时监控**：关注入选股票的基本面变化
- **风险预警**：监控ST风险、退市风险等

#### 实际应用注意事项
1. **数据源替换**：实际使用时需要接入真实的市场数据API
2. **时间序列**：考虑财务数据的报告期差异
3. **停牌处理**：需要处理停牌股票的权重调整
4. **分红除权**：考虑分红除权对股价和权重的影响

#### 扩展功能建议
1. **回测分析**：添加历史业绩回测功能
2. **风险指标**：计算组合的风险指标（波动率、最大回撤等）
3. **行业配置**：增加行业配置限制和优化
4. **ESG筛选**：考虑加入ESG评分筛选条件

---

*本文档详细说明了项目中各类数据文件的结构、内容和使用方法，为用户提供完整的数据理解和应用指导。*